# OpenAI / Proxy Configuration
# Se estiver usando OpenAI direto, use a chave da API e deixe a base_url padrão
OPENAI_API_KEY=your_openai_api_key_here

# Se estiver usando um proxy da OpenAI, configure a URL aqui
# Exemplo: http://localhost:3000/v1 ou https://seu-proxy.com/v1
# Se não configurar, usa o padrão: https://api.openai.com/v1
OPENAI_BASE_URL=http://localhost:3000/v1

# Server Configuration
SERVER_HOST=127.0.0.1
SERVER_PORT=8080

# Agent Configuration
AGENT_DEFAULT_PROMPT=You are a friendly and helpful voice assistant. You speak clearly and concisely.

# Flask API Configuration
API_HOST=0.0.0.0
API_PORT=8000
